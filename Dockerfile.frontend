FROM node:20-bookworm

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

RUN npm install -g @anthropic-ai/claude-code typescript vitest

RUN git config --system --add safe.directory '*'

WORKDIR /workspace
CMD ["/bin/bash"]
